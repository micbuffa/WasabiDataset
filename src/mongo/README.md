This folder provides tools to import in MongoDB and process in MongoDB the Wasabi dataset.

Script `import.sh` is the entry point. 
It loads the datasets and creates derived collections using the `*.js` files.
